# Руководство по переустановке сервера (Migration Guide)

Вы планируете отформатировать диск и восстановить проект "как есть". Поскольку ваша система состоит из двух частей (Дашборд и Инфраструктура в `/home/inno/compose`), важно сохранить обе.

## ⚠️ Что нужно сохранить (Бэкап)

Перед форматированием сохраните следующие данные на внешний диск или облако:

1.  **Папка кода дашборда**: `/home/inno/creationhub-dashboard` (бывш. `lovable-dashboard`)
2.  **Папка инфраструктуры и БД**: `/home/inno/compose` (Здесь лежит ваша база PostgreSQL!)
3.  **Конфигурации WireGuard**: `/etc/wireguard`

### Команда для создания архива (выполнять от root/sudo):
```bash
sudo tar -czvf full_backup.tar.gz /home/inno/creationhub-dashboard /home/inno/compose /etc/wireguard
```
*Сохраните файл `full_backup.tar.gz` на флешку или скачайте на другой компьютер.*

---

## 🛠 Процесс восстановления (после переустановки OS)

### 1. Подготовка системы
Установите Docker и Docker Compose:
```bash
# Пример для Ubuntu/Debian
curl -fsSL https://get.docker.com | sh
sudo usermod -aG docker $USER
# Перезайдите в систему, чтобы применились права группы
```

### 2. Восстановление файлов
Скопируйте ваш `full_backup.tar.gz` обратно на сервер в `/tmp`.
```bash
cd /
sudo tar -xzvf /tmp/full_backup.tar.gz
```
Это восстановит папки по их старым путям.

### 3. Запуск Инфраструктуры (База данных)
Сначала нужно запустить базу данных, так как Дашборд зависит от неё.
```bash
cd /home/inno/compose/compose
# Или где лежит ваш docker-compose файл инфраструктуры. 
# Судя по логам, он может называться base.yml или postgres.yml, запускайте через общий скрипт или:
docker compose -f base.yml -f postgres.yml up -d
# Проверьте, что контейнер creationhub-postgres запустился:
docker ps | grep postgres
```

### 4. Запуск Дашборда
```bash
cd /home/inno/creationhub-dashboard
# Пересборка и запуск
docker compose up -d --build
```

### 5. Проверка
Откройте дашборд в браузере. Если IP адрес сервера изменился, не забудьте обновить его в настройках дашборда (хотя мы настроили автоматическое определение).

## Важные замечания
*   **IP Адрес**: Если после переустановки у сервера будет другой IP (не `192.168.1.220`), вам может потребоваться обновить настройки в `docker-compose.yml` (если там остались жесткие привязки) или в `.env` файлах. Текущий код дашборда мы адаптировали к смене IP.
*   **WireGuard**: Восстановление папки `/etc/wireguard` вернет ваши ключи, но вам нужно убедиться, что модуль ядра WireGuard установлен (`sudo apt install wireguard`).
